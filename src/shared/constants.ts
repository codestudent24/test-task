export enum ROLE_ENUM {
  user = 'user',
  admin = 'admin',
}

export enum SUBJECTS_ENUM {
  math = 'math',
  geography = 'geography'
}

export const formStyle = {
  margin: '1rem auto',
  padding: '2rem',
  border: '1px solid #213547',
  borderRadius: '20px',
}